# System Files
.DS_Store
.ipynb_checkpoints/
*/.ipynb_checkpoints/

# Python
__pycache__/
*.py[cod]
*$py.class

# Data
home-credit-default-risk/

# Generated Outputs with Timestamps (Logs, CSVs, PNGs)
# Note: Keep non-timestamped files (e.g., final results) tracked if needed
# Explicitly ignore the non-timestamped submission file to avoid churn
outputs/predictions/submission_kernel02.csv
outputs/predictions/*_*.csv
outputs/*.csv
# Local comparison summaries (not needed for version control)
outputs/comparisons/*.csv
outputs/visualizations/*_*.png
outputs/logs/*_*.csv
outputs/logs/*.log
outputs/logs/oof_predictions*.csv
